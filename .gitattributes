# Normalize text files to LF in the repository.
* text=auto

# Unix shell scripts MUST stay LF (they run on macOS/Linux — CRLF breaks the shebang).
*.command text eol=lf
*.sh      text eol=lf

# Windows scripts stay CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
